This folder contains Hugging Face text/model space examples supported by the backend.

Current supported text space:

- huggingface/inference-playground

Usage:
- `POST /api/v1/generate/text`
- provider: `huggingface/inference-playground`

If you want, we can also add a helper script to run this text space directly from the command line.
